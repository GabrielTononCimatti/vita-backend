export const validatePerson = (person) =>
{
    if (typeof person.tipo_pessoa !== 'string' || !['PF', 'PJ', 'F'].includes(person.tipo_pessoa))
        throw new Error("Valor inválido para 'tipo_pessoa'. Deve ser 'PF', 'PJ' ou 'F'.");

    if(tipo_pessoa === 'PF')
    {
        const {cpf, nome} = novaPessoa;
        if (!cpf || !nome)
            return res.status(400).send({message: "Dados incompletos. 'cpf' e 'nome' são obrigatórios para pessoas físicas."});
    }
    if(tipo_pessoa === 'PJ')
    {
        const {cnpj, nome_fantasia, razao_social} = novaPessoa;
        if (!cnpj || !nome_fantasia || !razao_social)
            return res.status(400).send({message: "Dados incompletos. 'cnpj', 'nome_fantasia', 'razao_social' são obrigatórios para pessoas jurídicas."});
    }
    if(tipo_pessoa === 'F')
    {
        const {nome} = novaPessoa;
        if (!nome)
            return res.status(400).send({ message: "Dados incompletos. 'nome' é obrigatório para funcionários." });
    }

}